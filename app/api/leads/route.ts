import {NextResponse} from "next/server";import {getDb} from "../../../db";import {leads} from "../../../db/schema";
export async function POST(req:Request){try{const b=await req.json();const name=String(b.name||'').trim(),interest=String(b.interest||'').trim(),phone=String(b.phone||'').trim(),instagram=String(b.instagram||'').trim();const age=Number(b.age);const allowedInterests=['Jiu Jitsu Adulto','Jiu Jitsu Kids'];if(!name||!allowedInterests.includes(interest)||!phone||!Number.isInteger(age)||age<3||age>100)return NextResponse.json({error:'Dados inválidos'},{status:400});await getDb().insert(leads).values({name,age,interest,phone,instagram});return NextResponse.json({ok:true});}catch(e){console.error(e);return NextResponse.json({error:'Erro ao salvar'},{status:500});}}
