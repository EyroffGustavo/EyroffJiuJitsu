"use client";import {FormEvent,useState} from "react";export default function ProfessorLogin(){const[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/professor/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(r.ok)location.href="/professor";else{setError("Usuário ou senha inválidos.");setLoading(false)}}return <main className="loginPage"><section className="loginCard"><a className="brand brandLogo" href="/"><img src="/logo-eyroff-original.png" alt="Eyroff"/></a><span>ÁREA DO PROFESSOR</span><h1>ACESSAR TURMAS</h1><p>Entre com o usuário criado pelo administrador da Escola.</p><form onSubmit={submit}><label>Usuário<input name="username" required autoComplete="username"/></label><label>Senha<input name="password" type="password" required autoComplete="current-password"/></label><button className="button" disabled={loading}>{loading?"ENTRANDO...":"ENTRAR →"}</button><p className="loginError">{error}</p></form><a className="backLink" href="/">← Voltar ao site</a></section></main>}
